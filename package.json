{
  "name": "trailapp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Personal offline trail map — static web app, no build step.",
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/boot.smoke.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  }
}
